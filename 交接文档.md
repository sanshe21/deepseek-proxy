# DeepSeek 识图代理 — 项目交接文档

> 交接日期：2026-06-06
> 交接人：WorkBuddy
> 接收入：Project Workspace `C:\WBKJ\DeepSeek+GLM`

---

## 1. 项目概要

| 项目 | 说明 |
|------|------|
| **项目名** | DeepSeek 识图代理（deepseek-vision-proxy） |
| **别名** | DS-Vision Proxy / DeepSeek + GLM Vision Proxy |
| **定位** | 给纯文本模型 DeepSeek 装上"眼睛"的透明代理中间件 |
| **架构** | Express 透明代理 — GLM 看图描述 → 注入文本 → 转发 DeepSeek |
| **状态** | 已完成开发、测试、部署，当前为稳定运行版本 |
| **前项目路径** | `C:\Users\sss\WorkBuddy\2026-06-01-16-51-43\` |
| **现项目路径** | `C:\WBKJ\DeepSeek+GLM\deepseek-vision-proxy\` |

---

## 2. 架构原理

```
客户端 (WorkBuddy/其他)
       │
       │ POST (含 image_url)
       ▼
DeepSeek 识图代理 (Express :3001)
       │
       ├── 检测图片 → 调用 GLM-4.6V-Flash 识图
       │   └── 60 秒缓存去重（Map, MD5 key）
       │
       ├── 文字描述注入消息
       │   └── 清理所有 image_url（纯文本兼容）
       │
       └── SSE 流式转发到 DeepSeek Chat
```

**核心流程**：Client → Proxy → GLM（看图）→ 文字注入 → DeepSeek（回答）→ SSE 回流

---

## 3. 关键技术决策

### 3.1 模型选择

| 模型 | 用途 | 费用 | API |
|------|------|------|-----|
| **GLM-4.6V-Flash** | 图片识别 | **完全免费** ✅ | `open.bigmodel.cn/api/paas/v4` |
| **DeepSeek Chat** | 对话回答 | 按量（极便宜） | `api.deepseek.com` |

> 曾尝试过：豆包 doubao-seed（429 严重限流）→ 通义千问 VL（DashScope 无可用识图模型）→ 最终选定 GLM-4.6V-Flash

### 3.2 防御机制（三层）

| 层 | 机制 | 说明 |
|----|------|------|
| 🛡️ 队列串行化 | 1.5 秒请求间隔 | GLM 免费接口不能并发 |
| 🛡️ 指数退避重试 | 4s/8s/16s/32s/64s × 5 次 | 应对 429 限流（code 1305） |
| 🛡️ 图片缓存去重 | 60 秒 MD5 缓存 | 防止客户端重复发送同图 |

### 3.3 客户端适配

WorkBuddy 会重试发送同一张图片（日志可见 3-4 个独立请求，间隔 8-25 秒），缓存去重机制为此设计。

---

## 4. 修复的 Bug（重要经验）

详见 `README.md` 第 6 章，核心 BUG 摘要：

| Bug | 根本原因 | 修复方式 |
|-----|----------|----------|
| 历史图片累积 | `extractImages` 遍历所有消息 | 改为只从最新用户消息提取 |
| 无图也走识图 | `hasImages` 检测范围过大 | 改为只检测最新一条用户消息 |
| DeepSeek 400 报错 | `image_url` 残留 | 新增 `stripImages()` 严格清理 |
| 客户端重复发图 | WorkBuddy 网络重试 | 引入 60 秒图片缓存 |
| GLM 频繁 429 | 短时间密集调用 | 队列串行化 + 指数退避 |

---

## 5. 部署信息

| 项 | 详情 |
|---|------|
| **部署平台** | Sealos（国内区，杭州节点） |
| **容器镜像** | ghcr.io (GitHub Container Registry) |
| **镜像构建** | GitHub Actions — push 到 master 自动构建 |
| **公网地址** | `https://xzhymublbftq.sealoshzh.site` |
| **客户端接入点** | `https://xzhymublbftq.sealoshzh.site/v1` |
| **服务器规格** | 128Mi~256Mi 内存, 100m~500m CPU |
| **更新方式** | git push → GitHub Actions 自动构建 → Sealos 重启 |

### 环境变量

| 变量 | 说明 |
|------|------|
| `DEEPSEEK_API_KEY` | DeepSeek API Key |
| `DEEPSEEK_BASE_URL` | `https://api.deepseek.com` |
| `DEEPSEEK_MODEL` | `deepseek-chat` |
| `VISUAL_API_KEY` | 智谱 GLM API Key |
| `VISUAL_BASE_URL` | `https://open.bigmodel.cn/api/paas/v4` |
| `VISUAL_MODEL` | `glm-4.6v-flash` |
| `PORT` | `3001` |

---

## 6. 项目文件清单

| 文件 | 说明 |
|------|------|
| `README.md` | **主文档** — 架构原理、搭建步骤、核心代码解析、Bug 经验、故障排查（原 tutorial.md） |
| `deepseek-vision-proxy-guide.md` | **小白教程** — 面向非技术人员的 7 步图文指南 |
| `memory-log.md` | **开发日志** — 6 月 4 日完整开发记录（含问题、解决方案、Key 等） |
| `交接文档.md` | **本文档** — 项目交接与知识转移 |

> 注意：此项目为纯文档项目，核心代理代码（proxy.js）保存在 GitHub 仓库 `deepseek-proxy` 中，此处未包含源码文件。

---

## 7. 后续维护要点

1. **GLM 免费额度** — GLM-4.6V-Flash 免费但有每日/并发限制，429 过多时需考虑升版或备选模型
2. **缓存 TTL** — 当前 60 秒缓存，可根据实际使用情况调整
3. **日志监控** — Sealos 控制台可查看容器日志（建议开启 JSON 模式）
4. **Sealos 免费额** — 新用户有免费额度，小应用长期运行成本极低
5. **更新代码** — 改完 push 后需在 Sealos 手动重启应用，不会自动热更新

---

## 8. 附录：上下文关联

此项目与当前 WorkSpace `C:\WBKJ\DeepSeek+GLM` 的关系：
- 项目名称中的 "DeepSeek+GLM" 正是此代理方案的技术栈
- CC-Desktop 项目同样涉及 DeepSeek API 集成与云存储
- 国内网络环境下使用 npmmirror 镜像源的经验在两个项目中通用
